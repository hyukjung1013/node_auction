<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, user-scalable=no"/>
    <link rel="stylesheet" href="/css/mylist.css"/>
</head>
<body>
    <div id="container">
        <div class="wrapper" id="profile">
            <div>Hello! <b><%=user.dataValues.nickname%></b></div>
            <div>Deposit: <%=user.dataValues.money%> won</div>
            <a id="register-btn" class="btn" href="/">뒤로 가기</a>
        </div>

        <div class="wrapper" id="product">
            <div id="nav">내 낙찰 목록</div>
            <table id="table">
                <tr>
                    <th>상품명</th>
                    <th>이미지</th>
                    <th>낙찰가</th>
                </tr>
                <% for (good of goods) { %>
                    <tr>
                        <td class="good-name"><%=good.dataValues.name%></td>
                        <td class="img-wrapper"><img class="img" src="/uploads/<%=good.dataValues.img%>"/></td>
                        <td><%=good.dataValues.price%></td>
                    </tr>
                <% } %>
            </table>
        </div>
    </div>
</body>